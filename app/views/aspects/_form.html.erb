<%= form_with(model: aspect, local: true) do |form| %>
  <% if aspect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aspect.errors.count, "error") %> prohibited this aspect from being saved:</h2>

      <ul>
      <% aspect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :topic_id %>
    <%= form.text_field :topic_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
